name: CI

on: [ push ]

jobs:
  build:
    runs-on: ubuntu-latest
    container: docker.io/crowphp/phpunit:1.1
    steps:
      - uses: actions/checkout@v2
      - name: Install composer dependencies
        run: composer install --dev
      - name: Run PHPUnit
        run: php vendor/bin/phpunit
