name: CI

on: [ push ]

jobs:
  build:
    runs-on: ubuntu-latest
    container: docker.io/crowphp/phpunit:1.0
    steps:
      - uses: actions/checkout@v2
      - name: Install composer dependencies
        run: composer install --prefer-dist
      - name: Run PHPUnit
        run: php vendor/bin/phpunit
